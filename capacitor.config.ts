import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'example.com.fraudawareness',
  appName: 'Fraud Awareness',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
